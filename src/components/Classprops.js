import React, { Component } from "react";


class Classprops extends Component{

    render(){
        return <h1>class props : {this.props.name} aka {this.props.heroName}</h1>
    }

}

export default Classprops